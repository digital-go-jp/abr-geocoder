# DI 方針（分岐削減とテスタビリティ）

DI コンテナはユースケースに必要な依存（DB・キャッシュディレクトリ・ロガー・HTTP など）を集約します。これにより、ユースケースはインフラ選択の分岐から解放され、分岐はコンテナ初期化時に吸収されます。

- `AbrGeocoderDiContainer`: ジオコーディング用（DB 接続, キャッシュ, ロガー）
- `DownloadDiContainer`: ダウンロード用（ダウンロード/キャッシュディレクトリ, DB 接続, 既定の DCAT URL）

ユースケース側のメリット:

- どの DB/どのパスを使うかの if/else を排除
- モック注入で I/O を外してテスト可能

層の独立性:

- UseCases は Interface に依存せず（逆も同様）、Domain/Drivers への依存もコンテナ経由で完結

